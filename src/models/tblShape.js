//Create table tblShape have id: int + name: nvarchar + image: nvarchar
class tblShape {
    constructor(id, name, image) {
        this.id = id;
        this.name = name;
        this.image = image;
    }
}